[package]
name = "git-hash"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Object IDs and hash algorithms (pure Rust SHA-1 / SHA-256)"

[dev-dependencies]
proptest = "1"
